# bunpo wizard

1. To use a **Namespace Import**, e.g. `import * as z from 'zod';` for importing z from zod. I still need to learn more about exactly how it works, but apparently it allows perfect tree shaking by bundlers like vite.
2. Since `Express 5` Implicitly wraps ALL middleware passed into routes in a try/catch block, technically I don't need to write a try/catch block in my middleware (that'd just be writing a nested try/catch - there are reasons we'd still do this, but I'm not good enough to do that stuff yet lol). However, for now I'll continue writing these nested try/catch blocks for practice writing try/catch blocks!
3. Welp, I learned today that I was using the conventional commit prefix `chore:` wrong all this time. I guess when you add anything that changes the product itself, `feat:` is better. `Chore:` is for changes that don't affect the production code. Oops.
4. Learned about `Zod` methods and the different kinds, such as `Base Types` and `Refinements`. Base types start the chain, and refinements continue the chain (as a quick summary). Here's a little cheat sheet I had gemini make me lol:

```markdown
## 1. Base Types (Start the chain)

_Standalone constructors—cannot be chained onto other types._

- `z.string()` / `z.number()` / `z.boolean()`
- `z.enum(["A", "B"])` / `z.nativeEnum(TypeScriptEnum)`
- `z.object({})` / `z.array()`

## 2. Type Refinements (Chain onto specific types)

_Methods that only exist on matching Base Types._

- **Strings:** `.trim()`, `.min()`, `.max()`, `.email()`, `.toLowerCase()`
- **Numbers:** `.int()`, `.positive()`, `.negative()`, `.gte()`

## 3. Universal Modifiers (Chain onto ANY type)

_Global methods used to modify the final output or metadata._

- `.optional()` (adds `| undefined`)
- `.nullable()` (adds `| null`)
- `.default(value)` (fallbacks if input is missing)
- `.describe("text")` (adds metadata)
```

5. I learned to use the user's id as a foreign key for something like a battle is because the user's id never changes, BUT they might want to change their username at some point! So if we used the username as the foreign key, postgres would have to update a LOT of rows if the user changed their username.

6. When you `POST` a resource, according to REST conventions, you should always send back the resource you just created.
