<details>
<summary>🔵 <b>Setters and Getters</b></summary>

### 🟣 The core difference between using accessor properties VS data properties for making setters and getters
This is a question I wrote out that helped me understand the concept:
`so to my understanding, explicit method getters and setters are value attributes that have a function as their value. And get and set attributes of accessor properties are also attributes that hold a function to run. So is the main difference that with the explicit method way, which is of course via a data property, only one function can be assigned, therefore requiring you to write out two properties, one for getting and one for setting, but with an accessor property, you can bake both functions (one for getting and one for setting) right into that one property?`


</details>